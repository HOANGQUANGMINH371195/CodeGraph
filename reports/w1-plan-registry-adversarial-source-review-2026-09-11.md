# W1 execution-plan adversarial checks

Source gate ready before edits. Re-read clean Grit
`0f3c9d04abe9525b1884f3a0ade890e54a6d9ffe`, src/db/sqlite_store.rs:48–80,
480–515: write-lock before read, independent connections and durable winner.
Reference tests inspected, not executed. Local execution_plan, execution_receipt,
artifact lookup and analysis_run read paths inspected.

Gap: plan registration checks candidate descriptor but unlike ArtifactRepository
does not validate the referenced analysis run on read. Corrupted DBs with both
foreign keys and the delete guard bypassed could register an orphan candidate. Add the
same run identity/snapshot validation within the existing IMMEDIATE transaction;
never repair corrupted data implicitly. No migration or dependency change.

Tests planned: plan vs mismatched receipt has exactly one insertion; plan vs
cancel either registers before cancellation or is unavailable afterward, never
revives the submission. Reopen validates durable rows. Corrupted plan version,
identity/task linkage must block read/replay/receipt; orphan analysis run must
block first registration. Use separate Store connections and owned fixture DBs.
Skills rust-router and m07-concurrency. No source copied, no runtime/account jobs.
These checks are not launch-once authority or proof of hostile DB tamper detection
in general; correctly shaped forged data still needs host authentication.

Initial fixture could not remove the run: existing analysis_runs_no_delete trigger
correctly rejected the mutation. Re-read V4 and explicitly remove only that guard
in the owned corruption fixture, then disable foreign keys on its connection.
This is defense-in-depth for an already corrupted DB, not a normal API bypass.

Regression then reproduced acceptance of the orphan before the fix (test exit
101). Registration now checks analysis-run descriptor/version/identity/snapshot
inside its write transaction. Full foundation exited 0: 146 Rust + 11 Node pass,
with the existing subprocess-only helper ignored by discovery and invoked by
its parent. Three new tests cover orphan rejection, three plan corruptions and
16 independent-connection race fixtures (eight receipt, eight cancellation).
Reopen asserts durable serial outcomes. Format and PLAN mirror checked.
Only owned temporary test data was deleted to simulate corruption; no reference
or product data was removed. The fixture is regenerated by rerunning the test.
